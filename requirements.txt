# Runtime deps for local dev, CI, and the Modal web image.
# Heavy GPU deps (torch, transformers, the describe-anything package) are installed
# inside the Modal DAM GPU image only (see modal_app.py), not needed for the web layer.

# Web / serving
fastapi[standard]>=0.115
uvicorn>=0.30
httpx>=0.27
openai>=1.55            # OpenAI-compatible client for the NVIDIA NIM reasoner
python-dotenv>=1.0

# Database (self-hosted Supabase / Postgres on Railway)
asyncpg>=0.29

# Detection (YOLO26) + image handling
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
numpy>=1.26
Pillow>=10.0.0
pyyaml>=6.0
